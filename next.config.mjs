/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['192.168.50.31'],
  // turbopack: {
  //   root: __dirname,
  // },
};

export default nextConfig;
